"""GUI tab modules. Each exposes ``make_tab(project, scan) -> QWidget`` and a
``TAB_META`` dict so the single-window app can discover and embed it. Tabs are
added in Phase 4+ (see ``PackageDraft2/IMPLEMENTATION.md``)."""
